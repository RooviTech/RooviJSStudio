apply plugin: "java"
apply plugin: "application"

repositories {
	mavenCentral()
}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
}

dependencies {

}

task wrapper(type: Wrapper) {
	gradleVersion = "1.12"
}